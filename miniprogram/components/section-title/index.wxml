<view class="sec">
  <view class="sec-left">
    <text class="sec-title display">{{title}}</text>
    <text wx:if="{{sub}}" class="sec-sub">{{sub}}</text>
  </view>
  <view wx:if="{{more}}" class="sec-more" bindtap="onMore">
    {{more}}<van-icon name="arrow" size="24rpx" />
  </view>
</view>
